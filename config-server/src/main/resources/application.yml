server:
  port:0
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///E:/N/escritorio/workspace-sts/config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka